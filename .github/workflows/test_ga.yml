on: [push]
jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ls -Al / > root.txt
      - run: tree
      - run: git add root.txt
      - run: git commit -m "test"
